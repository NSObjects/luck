# === OS / Editors ===
.DS_Store
Thumbs.db
*.swp
*.swo
*.swn
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.vscode/
.idea/
*.code-workspace

# === Env files (keep template only) ===
.env
.env.*
!.env.example

# === Frontend (web/) ===
web/node_modules/
web/dist/
web/.vite/
web/.cache/
web/coverage/
web/.eslintcache

# Vercel local metadata
.vercel/

# === Backend (Go) ===
# compiled binaries / build outputs
backend/bin/
# embedded frontend build output (generated by Vite)
backend/web/dist/
# test/coverage
backend/coverage/
coverage.out
*.coverprofile

# common Go artifacts (keep broad but safe)
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# === SQLite data (both root / backend) ===
data/
backend/data/

# === Misc ===
# lockfiles are kept on purpose (package-lock.json, go.sum)
# add project-specific ignores below if needed
